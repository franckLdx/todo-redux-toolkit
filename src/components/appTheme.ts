import { DefaultTheme } from "styled-components";

export const appTheme: DefaultTheme = {
  container: {
    justifyContent: "around",
    color: "purple",
  },
  label: {
    paddingX: 2,
    paddingY: 2,
    shape: "rounded",
    color: "lightGray",
  }
}